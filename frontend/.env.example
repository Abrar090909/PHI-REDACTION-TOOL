# MedRedact Frontend — Environment Variables
# Copy this to .env.local and fill in your values.

# URL of the deployed backend (Render).
# Leave empty in local dev — Vite proxy handles it automatically.
# Example: VITE_API_URL=https://medredact-api.onrender.com
VITE_API_URL=
